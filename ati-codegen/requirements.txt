# ------------------------------------------------------
# 依赖清单（适配 Qwen2.5-Coder-7B + LoRA + GPU + CUDA12）
# 安装方法：
# 1. 先配置阿里云镜像（必须）
# pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/
# pip config set global.trusted-host mirrors.aliyun.com
#
# 2. 创建并激活环境
# conda create -n qwen python=3.11
# conda activate qwen
#
# 3. 先安装 PyTorch（必须手动执行）
# pip install torch==2.5.0 torchvision==0.20.0 torchaudio==2.5.0 --index-url https://download.pytorch.org/whl/cu121
#
# 4. 一键安装所有依赖
# pip install -r requirements.txt
# ------------------------------------------------------

# Hugging Face 核心
transformers>=4.40.0
datasets>=2.18.0
peft>=0.10.0
accelerate>=0.28.0
evaluate>=0.4.0

# 推理引擎 vLLM (Linux + GPU only)
vllm>=0.4.0

# 数据处理
pandas>=2.0.0
pyarrow>=15.0.0
numpy>=1.24.0

# 工具
tqdm>=4.65.0
sentencepiece>=0.1.99
protobuf>=3.20.3
tiktoken>=0.6.0

# 训练日志可视化
tensorboard>=2.14.0

# 其他依赖
matplotlib>=3.7.0